FROM nginx:alpine

# Copy the static web frontend and compiled WebAssembly to the default Nginx HTML path
# Uses 'web' instead of '../web' because docker-compose context is the repo root
COPY web /usr/share/nginx/html

# Expose port 80 for UI traffic
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
