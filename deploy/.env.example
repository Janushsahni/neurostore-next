# ═══════════════════════════════════════════════════════════════
# NeuroStore Enterprise Environment Configuration
# Generate secure, high-entropy values for production deployment
# ═══════════════════════════════════════════════════════════════

# Database Configuration
POSTGRES_USER=neuro_admin
POSTGRES_PASSWORD=generate_a_secure_random_password_here
POSTGRES_DB=neurostore_production
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@neurostore-db:5432/${POSTGRES_DB}

# Redis Configuration
REDIS_URL=redis://neurostore-redis:6379

# Gateway Cryptography Secrets (CRITICAL)
# Must be at least 32 bytes, high entropy
METADATA_SECRET=generate_a_very_secure_random_32_byte_string_for_metadata
JWT_SECRET=generate_a_very_secure_random_32_byte_string_for_jwts

# Control Plane Cryptography Secrets
MACAROON_SECRET=generate_a_very_secure_random_32_byte_string_for_macaroons

# Rust Logging
RUST_LOG=info,neurostore_gateway=debug
