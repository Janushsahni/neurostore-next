[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/Dockerfile.gateway"

[deploy]
healthcheckPath = "/readyz"
healthcheckTimeout = 30
startCommand = "neurostore-gateway"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
